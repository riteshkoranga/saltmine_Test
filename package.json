{
  "name": "shopping-cart-assignment",
  "version": "1.0.0",
  "description": "Shopping cart assignment for interviews",
  "main": "index.js",
  "scripts": {
    "test": "node test.js",
    "serve-products": "json-server --watch db.json --port 3001 --routes routes.json"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  },
  "dependencies": {
    "axios": "^1.7.9"
  }
}
